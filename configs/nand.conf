# NAND geometry. Loosely modeled after a consumer TLC SSD, scaled down so
# full sweeps finish in seconds rather than hours.
page_size_bytes = 4096
pages_per_block = 256
blocks_per_die = 512
dies_per_channel = 2
channels = 4
t_read_us = 50
t_program_us = 600
t_erase_us = 3000
